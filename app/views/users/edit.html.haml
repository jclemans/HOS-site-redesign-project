%h2= h @user.djname

.col-sm-4
  = simple_form_for @user do |f|
    = f.error_messages
    = f.input :djname, label: "DJ Name"
    = f.input :password
    = f.input :password_confirmation
    = f.button :submit, ('Update'), :class => 'btn btn-success'
